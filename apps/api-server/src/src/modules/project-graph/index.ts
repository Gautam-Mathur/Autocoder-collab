/**
 * Project graph (Task #18) — barrel.
 *
 *   buildProjectGraph(files) → ProjectGraph
 *   verifyProjectGraph(graph, files) → GraphVerificationResult
 *   formatVerificationErrors(result) → string[]
 */
export * from './graph-builder.js';
export * from './verifier.js';
export * from './impact.js';
